# Artifacts

Optional **local** copies of the composed weekly pulse (Markdown or plain text) before or after MCP delivery. Useful for debugging composition without re-querying stores.

Contents of this directory are **git-ignored** by default.
